# Build prompt 10 — website bridge (G6)

Paste into a fresh Claude Code session at the root of `upriver-clone`. PRECONDITION: the e2e synthetic run's PR is merged (the littlefriends profile is your acceptance fixture) and Cowork's run evaluation has not amended the coverage map in ways that conflict — check `.planning/intake-profile-engine/07-e2e-run-report.md` exists and skim its follow-ups first; if it flags coverage-map changes, STOP and report before building.

---

You are building one component: the profile→website bridge per `.planning/intake-profile-engine/specs/10-website-bridge-spec.md` — read it first and in full; it is the source of truth and your file-ownership boundary. Then read: `08-capability-audit.md` (G6), the existing pipeline seams you will modify (`packages/cli/src/commands/design-brief.ts`, `commands/scaffold/index.ts`, `commands/clone.ts` — find every `loadAuditPackage`/`readIntake` content read), `packages/schemas/src/coverage-map.ts` + its cross-check tests, `packages/cli/src/generate/engine.ts` (GENERATABLE + scope handling), one existing doc spec end-to-end (e.g. `specs-reference/ai-operating-system/10-website-audit-spec.md`) as the format model for the two specs you author, and `packages/scaffold-template/src/styles/global.css` for the tokens mapping.

Build in this order, compile + test after each phase: (1) author specs 19 (web-PRD) and 20 (design-system) in `specs-reference/` — these are writing, not code; match the house 8-section format exactly; (2) the two `COVERAGE_MAP` rows + schema leaf additions the spec names, cross-check suite green with updated counts, both rows HV-gated on `goals.engagementScope.websiteScope`; (3) `web-bridge/inputs.ts` (`resolveWebInputs`, profile-first / audit-package fallback / merged) with the four-case unit tests; (4) the three command seams — swap ONLY the input reads, no other diff in those files; (5) `--web` scope in the generate command, excluded from `--all` (tested); (6) tokens.json emission + scaffold-template mapping doc + smoke test; (7) the §D live acceptance: verify websiteScope, `generate littlefriends --web` (PRD + design-system, gates reviewed by you under Joshua's standing synthetic authorization ONLY if the littlefriends profile is still the synthetic one — if a real client profile has replaced it, STOP at the gates and report), then `design-brief`/`scaffold` with the three grep-proven profile-sourced facts, and the audreys byte-identical regression.

Hard rules: a client with audit-package and no profile behaves byte-identically (regression snapshot before any seam change); no external design-tool calls — Claude Design/Pinterest/Gemini steps are `[OPERATOR ACTION]` lines in the generated docs; the new-site build itself is NOT generated (the web-PRD + design system + scaffold are the deliverables; bespoke builds stay operator-driven Claude Code work); schemas changes limited to exactly what the spec names.

Definition of Done is the spec's, verbatim. Branch `build/10-website-bridge`, ownership check via `git diff --name-only` before finishing, changelog updated (deviations, acceptance transcript, follow-ups: new-site build playbook, web-PRD→proposal linkage, stage-field interaction). Stop after the PR; do not merge.
