# Build prompt 11 — generation hardening

Paste into a fresh Claude Code session at the root of `upriver-clone`. PRECONDITION: PR #34 (the e2e run record) is merged to main, so the e2e evaluation, run report, and the committed `clients/littlefriends/docs/*` are on main — you need those generated docs as test fixtures. `git pull` first; confirm `.planning/intake-profile-engine/11-e2e-evaluation.md` and `clients/littlefriends/docs/doc-07-faq-bank.md` exist. If not, STOP and report.

---

You are building one component: generation hardening per `.planning/intake-profile-engine/specs/11-generation-hardening-spec.md` — read it first and in full; it is the source of truth and your file-ownership boundary. Then read `11-e2e-evaluation.md` (the why) and `07-e2e-run-report.md` findings D8/D9 (the symptoms), and the modules you'll edit: `packages/cli/src/generate/prompt-builder.ts` (line ~66 injects full upstream `content` — this is the bug), `engine.ts` (dry-run size logging at ~149, digest computation goes before prompt assembly), `runner.ts` (staging cwd, `findGeneratedFile`, the cache-miss path), and the `UPRIVER_LLM_NO_CACHE` handling in the claude-cli wrapper.

This is hardening, not redesign: do NOT change `packages/schemas`, the coverage-map, the DAG edges, the gating, or the merge model — the evaluation vindicated all of them. Only how upstream docs are *passed into the prompt* changes, plus three resilience guards.

Build in order, compile + test after each: (1) `generate/upstream-digest.ts` — the v1 deterministic structural extract (headings + first sentence per section + lists + ~1,500-word char cap), cached at `docs/.digests/<id>.md` keyed by upstream content hash, with tests proving it stays under cap and preserves structure; do NOT build the v2 LLM-summarized digest (note it deferred). (2) Wire it into `engine.ts` (compute digests before prompt assembly) and `prompt-builder.ts` (upstream section uses `digest` not `content`); keep a `--full-upstream` debug escape. (3) The headline regression test: assemble doc-08's user prompt using the REAL committed doc-01/02/03/07 as digest inputs and assert it lands under the F2 ceiling — this is the test that proves the actual bug is dead. (4) F2: extend `--dry-run --all` to print a per-doc prompt-size table with OK/FAIL and exit non-zero if any doc exceeds the ceiling. (5) F3 + F4 in `runner.ts`: cache-hit-with-no-file auto-forces a fresh call; absolute-path writes get relocated or precisely errored; and set `UPRIVER_LLM_NO_CACHE=1` on the `e2e-littlefriends.sh` resume paths.

Definition of Done is the spec's, verbatim — especially the doc-08-under-ceiling regression test. Branch `build/11-generation-hardening`, ownership check via `git diff --name-only` before finishing (schemas must show zero changes), changelog mapping each fix to its finding (F1→D8, F3+F4→D9, F2→prevention). Stop after the PR; do not merge.
