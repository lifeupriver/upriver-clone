# Build prompt 14 — fidelity hardening (provenance-aware generation)

Paste into a fresh Claude Code session at the root of `upriver-clone`. PRECONDITION: the branch `build/14-fidelity-hardening` exists (local or origin) and carries the spec and the plan. Run `git fetch --all && git checkout build/14-fidelity-hardening`, then confirm both files exist: `.planning/intake-profile-engine/specs/14-fidelity-hardening-spec.md` and `.planning/intake-profile-engine/plans/2026-06-10-14-fidelity-hardening-plan.md`. If either is missing, STOP and report. Quit GitHub Desktop first if it is running (it holds `.git/index.lock` and will break the commits).

---

You are building one component: fidelity hardening per `.planning/intake-profile-engine/specs/14-fidelity-hardening-spec.md` — read it first and in full; it is the source of truth and your file-ownership boundary. Then read the implementation plan at `.planning/intake-profile-engine/plans/2026-06-10-14-fidelity-hardening-plan.md` — it decomposes the spec into 12 TDD tasks with the exact code, tests, commands, and commits; execute it task-by-task in order (use superpowers:executing-plans or superpowers:subagent-driven-development), checking off steps as you go. For the why, read `.planning/intake-profile-engine/13-e2e-live-evaluation.md` (on the `e2e/littlefriends-synthetic-v2` branch or in PR #37 if not on this branch) and `12-e2e-live-report.md` findings D1, D3, G, and §3.

The shape of the work: one unverified recon field (`identity.category` = "Montessori preschool") rewrote the client's identity through doc-01 into ~10 downstream docs because `profile-slice.ts` strips provenance before generation; most sessions never see `identity.publicName` (i06 confabulated "JCC"); extraction never wrote the three fields the corpus states (the leak's root cause); the readiness gate hides provisioning blockers; and the runner's no-file failure class needs one self-healing retry. Six fixes, P1–P6, all surgical.

This is hardening, not redesign: the merge model (`packages/schemas/src/merge.ts`), recon adapters, dashboard, DAG edges, and the e2e script's phase structure are out of bounds. `coverage-map.ts` changes are DATA only (requiresFields additions, SOURCE_EXPECTATIONS.transcript additions) — no shape changes, nothing added to `requiresHvVerified`. Do NOT touch the 27 generated artifacts in `clients/littlefriends/docs/` — content fixes are explicitly out of scope (operator decision: they stay as the eval-branch run record; the next e2e run exercises these fixes).

Build in the plan's order, compile + test after each task, one commit per task with the plan's commit messages: (1–2) P1 provenance tag + universal hedge instruction; (3–5) P2 publicName everywhere + identity-assert module + engine/command wiring; (6–7) P3 transcript expectations + extraction recall push; (8) P4 slice additions for doc-02/04/12/16; (9–10) P5 provisioning projection + `--strict-provisioning` + e2e script adoption; (11) P6 runner self-heal; (12) integration — full build, ownership diff, the F2 dry-run regression against the local littlefriends client, the ONE live acceptance step (a single `profile extract-transcript` run against the synthetic corpus, backup/restore around it, candidates must appear for `identity.category`, `identity.socialHandles`, `voice.bannedVocabulary`), spec DoD + changelog.

Two known-fallout notes the plan details: adding `identity.publicName` to every deliverable will surface CLI test fixtures that need the field filled (add it with the file's `env()` helper — the littlefriends fixture profile already has it); wiring the identity assert requires `engine.test.ts`'s `writingCall` default content to include the fixture's publicName.

Definition of Done is the spec's, verbatim — every checkbox verified, not assumed. Ownership check via `git diff --name-only main...HEAD` before finishing (only spec-owned files + tests + planning docs). Changelog maps each fix to its finding (P1→Montessori metastasis, P2→D3+placeholders, P3→Disclosure E, P4→marker false positives, P5→Finding G, P6→D1), with the live-acceptance extraction counts recorded. Push the branch and open the PR per the plan's Task 12; stop after the PR — do not merge.
