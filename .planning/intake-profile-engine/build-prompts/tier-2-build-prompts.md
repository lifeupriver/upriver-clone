# Tier-2 build prompts: specs 03, 04, 05 (parallel sessions)

Three independent Claude Code sessions, each at the root of `upriver-clone` (pull main first — must include PR #25, commit `6dee4ff` or later). They are parallel-safe by file ownership; each prompt below is self-contained. Run all three at once or in any order.

**Shared rules for every session (each prompt repeats the essentials):**
- Your spec's "File ownership" section is a hard boundary. Before finishing, run `git diff --name-only` and confirm every changed file is inside your owned set (plus your spec's changelog). If you believe you must touch a file you don't own, STOP and report instead.
- Work on a branch named `build/<spec-number>-<short-name>`; commit there. Do not merge other tier-2 branches into yours.
- Shared modules (`packages/schemas`, `packages/cli/src/generate/*` except where spec 03 owns `data-source.ts`) are import-only.
- Phased: one file at a time, `pnpm --filter @upriver/cli typecheck` (or dashboard equivalent) after each. Tests beside each module, `claudeCliCall` and network mocked in unit tests.
- If your spec proves wrong against reality, record the deviation in the spec's changelog and continue if the fix is local; STOP and report if it crosses your ownership boundary.
- Finish by updating your spec's changelog (BUILT entry, deviations, transcripts, follow-ups) and reporting: file list, test summary, acceptance transcript, deviations.

---

## Session A — paste this for Build Spec 03 (profile commands)

You are building one component: the `upriver profile` operator commands (show, set, verify, conflicts, pull, push) and the canonical-Supabase default flip. Read first, in order: (1) `.planning/intake-profile-engine/specs/03-profile-commands-spec.md` — your spec and hard file-ownership boundary; (2) `specs/02-generate-spec.md` changelog — the follow-up notes this build implements; (3) `packages/cli/src/generate/profile-io.ts`, `profile-merge.ts`, `data-source.ts` — the modules you import (you own ONLY `data-source.ts` for the one-line default flip); (4) `packages/schemas/src/coverage.ts`, `hv.ts`, `merge.ts` — readiness, HV predicate, merge semantics; (5) `packages/cli/src/commands/profile/import.ts` and `commands/generate.ts` — command conventions (thin command over pure module, per the engine/gate pattern). Build per your spec §2 layout: thin oclif commands in `commands/profile/`, logic in a new `packages/cli/src/profile/` (paths, show-model, render, mutate, sync). Key semantics: `set` never flips verified (set and verify are two acts); `verify` is the only path past an HV gate and requires a non-null value; `pull`/`push` merge via `mergeProfiles` and never silently overwrite; the data-source default flips to `'supabase'` with a clear env-naming error message and the `UPRIVER_DATA_SOURCE=local` escape hatch. Phases: paths + tests → show-model + render + show → mutate + set/verify/conflicts → sync + pull/push → the flip → live acceptance. Live acceptance (required): verify the 10 HV fields blocking doc-02 for littlefriends (`--evidence "fixture hand-fill"`), then show `upriver generate littlefriends --doc doc-02 --dry-run` reporting READY — the gate demonstrably opens. Commit the before/after `profile show` transcript into the spec changelog. DoD is your spec §3, verbatim, all green. Do not touch recon, intake-reader, the dashboard, or schemas. Branch: `build/03-profile-commands`.

---

## Session B — paste this for Build Spec 04 (recon)

You are building one component: `upriver recon`, automated profile fill. Read first, in order: (1) `.planning/intake-profile-engine/specs/04-recon-spec.md` — your spec and hard file-ownership boundary; (2) `01-prd.md` §4.1; (3) `packages/schemas/src/coverage-map.ts` (`SOURCE_EXPECTATIONS.recon` — your fill target) and `merge.ts` (recon is lowest precedence; you fill gaps, never overwrite, never verify); (4) `packages/core/src/firecrawl/client.ts` — the scrape client you reuse; (5) `packages/cli/src/generate/profile-io.ts` + `data-source.ts` — import-only write path; (6) `packages/cli/src/util/claude-cli.ts` — read-only `--json-schema` extraction calls. Build per your spec §2 layout: `commands/recon.ts` thin, `recon/` with types, run orchestration, merge-candidates, report, and six adapters (website, gbp, socials, geo, serp, secret-shopper-scaffold). Adapter contract per spec §1: gather evidence into `clients/<slug>/recon/<id>/` via the data source, extract candidates with dot-paths + recon source + per-adapter confidence + evidence strings, structurally validated via `--json-schema` against the relevant section shape. Partial adapter failure must not abort the run. Phases: types + merge-candidates + tests → run + report + tests → website adapter (fixture-tested) → gbp, socials → geo, serp → secret-shopper scaffold → live acceptance. Live acceptance (required): `upriver recon littlefriends --adapters website,gbp` against the real site (needs FIRECRAWL_API_KEY); commit evidence + report transcript + the recon-vs-hand-fill agreement table into the spec changelog. Hard rules: no recon path ever writes `verified: true` (test it); unit tests use recorded fixtures, zero live network. DoD is your spec §3, verbatim. Do not touch profile commands, intake-reader, the dashboard, or schemas. Branch: `build/04-recon`.

---

## Session C — paste this for Build Spec 05 (ClientIntake migration)

You are building one component: the `ClientIntake → Profile` migration. Read first, in order: (1) `.planning/intake-profile-engine/specs/05-intake-migration-spec.md` — your spec and hard file-ownership boundary (you own intake-reader, the five consumer call sites' read lines only, the dashboard intake layer, and `commands/profile/migrate-intake.ts` — disjoint from spec 03's profile files); (2) `01-prd.md` "Relationship to the existing intake/interview layer"; (3) `packages/core/src/types/intake.ts` and `packages/schemas/src/sections/auditDecisions.ts` — verify the mirror field-for-field FIRST; if they drift, stop and changelog, do not adapt; (4) `packages/cli/src/util/intake-reader.ts` and its three CLI consumers (`commands/fixes/plan.ts`, `commands/clone.ts`, `commands/improve/index.ts`); (5) `packages/dashboard/src/lib/intake-writer.ts`, `lib/fs-reader.ts` (readIntake), `pages/api/intake/[slug].ts`, `pages/api/intake/[slug]/lock-scope.ts` — the dashboard layer you repoint; (6) `packages/cli/src/generate/profile-io.ts` — the write path you import. Mechanics per spec §1: a one-shot idempotent `profile migrate-intake <slug>` (wrap-as-unit operator envelope, merge, freeze legacy with a `.migrated` marker, never delete); a compatibility `readIntake` (profile-first, legacy fallback, same return shape — sync→async is the one permitted call-site change, applied at all consumers if taken); dashboard dual-write (profile + legacy mirror) with byte-identical API contracts. Phases: mirror verification → migrate plan + command + tests → compat reader + consumer call sites → dashboard repoint + route-contract regression tests → live acceptance (seed a legacy intake.json for littlefriends, migrate, prove the compat read and the frozen original). DoD is your spec §2, verbatim. Do not build any other profile subcommand, touch recon, or modify schemas. Branch: `build/05-intake-migration`.

---

## After the three sessions

Merge order if all pass review: 03 → 05 → 04 (03 flips the data-source default that 04's live run will use; 05's dashboard changes are independent; 04 last because its live transcript is most env-dependent). Any conflicts at merge should be trivial (`package.json` script additions at most) given the ownership boundaries. Bring all three results back to Cowork for the single tier-2 review gate before merging.
