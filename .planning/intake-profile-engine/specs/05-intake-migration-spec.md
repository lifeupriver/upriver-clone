# Build Spec 05: `ClientIntake → Profile` migration

Component: M4's migration half — fold the legacy `clients/<slug>/intake.json` into the profile's `auditDecisions` section, swap the consumers, freeze the legacy artifact. Per the PRD's "Relationship to the existing intake/interview layer" section, executed exactly.
Built: tier-2 batch, parallel with specs 03 and 04, fresh Claude Code session.
Source of truth: `01-prd.md` "Relationship…" section; `packages/schemas/src/sections/auditDecisions.ts` (already mirrors `ClientIntake` shapes). Where this spec is more specific, it wins.

## File ownership (parallel-build boundary)

This session OWNS: `packages/cli/src/commands/profile/migrate-intake.ts`, `packages/cli/src/util/intake-reader.ts` (the compatibility-reader rewrite), the five CLI consumers' import lines (`commands/fixes/plan.ts`, `commands/clone.ts`, `commands/improve/index.ts` — touch ONLY the intake-read call sites), and the dashboard intake layer (`packages/dashboard/src/lib/intake-writer.ts`, `lib/fs-reader.ts` readIntake only, `pages/api/intake/[slug].ts`, `pages/api/intake/[slug]/lock-scope.ts`). It must NOT touch: `commands/profile/{show,set,verify,conflicts,pull,push}.ts` or `packages/cli/src/profile/` (spec 03's), `packages/cli/src/recon/` (spec 04's), `packages/schemas` (read-only), `generate/*` (read-only imports). NOTE: spec 03 also creates files under `commands/profile/` — disjoint filenames, no conflict; do not create an index or barrel there.

## 1. Mechanics (the PRD's four migration steps, made exact)

1. **Schemas already shipped `auditDecisions`** (step 1 done in Build 01). Verify the mirror: field-for-field against `@upriver/core`'s `ClientIntake` minus the version envelope; if drift is found, STOP and changelog — do not adapt silently (schemas is read-only this tier).
2. **`upriver profile migrate-intake <slug>`** — one-shot: read legacy `intake.json` (via the data source); wrap as a whole-section operator-sourced envelope (`profileFieldZ(auditDecisionsZ)` per spec 01 §6 — auditDecisions is envelope-wrapped as a unit, already-confirmed client intent); merge into the profile via `mergeCandidate` (conflict if the profile already has a differing auditDecisions — report, queue, do not overwrite); write `intake.json.migrated` marker file alongside the frozen original (the original is NOT deleted — deletion happens after one full client cycle, per the PRD); print before/after. `--all` iterates every slug with an `intake.json`. Idempotent: a second run on a migrated slug is a no-op with a notice.
3. **Compatibility reader.** Rewrite `packages/cli/src/util/intake-reader.ts`'s `readIntake(slug)` to: read the profile's `auditDecisions` value if present, else fall back to legacy `intake.json`, returning the same `ClientIntake` shape either way (synchronous signature today — it may need to become async; if so, the five call sites change `readIntake(slug)` → `await readIntake(slug)` and that is the entire permitted change at those sites). Consumers keep compiling with their existing types; zero behavioral change when only legacy exists.
4. **Dashboard repoint.** `intake-writer.ts`'s `writeIntake` → writes the profile's `auditDecisions` (same merge path; revision bump) AND mirrors to legacy `intake.json` during the freeze period (dual-write, so an un-migrated CLI consumer on an old checkout still functions); `fs-reader.ts`'s `readIntake` → same compat logic as the CLI's. The two API routes keep their request/response contracts byte-identical — this is a storage repoint, not an API change. The dashboard's intake form keeps working untouched (per the PRD: the static layer persists until M6 is proven).

Out of scope: deleting `intake.json`, renaming the `ClientIntake` type in `@upriver/core` (it retires later; removing it now would ripple), touching `interview-guide.md`/FormSpec anything, the chatbot.

## 2. Definition of Done

- [x] Root `pnpm build` clean; cli AND dashboard test suites green (CLI 265/265, dashboard 15/15)
- [x] Unit tests: migrate plan (fresh, conflicting, idempotent re-run); compat reader (profile-only, legacy-only, both-prefer-profile, neither); dual-write mirrors correctly; API route contract regression (route tests added — none existed — incl. a three-way byte-identity oracle: POST body ≡ reconstruct-from-profile ≡ legacy mirror)
- [x] Live acceptance: seeded a legacy `intake.json` for littlefriends, ran `migrate-intake littlefriends`, demonstrated filled auditDecisions via `readProfile` node one-liner (spec 03's `show` not built here), confirmed `readIntake('littlefriends')` returns the migrated data with `intake.json` frozen + `.migrated` marker (see Changelog transcript)
- [x] The CLI consumers (three use `readIntake`: `fixes/plan`, `clone`, `improve/index`) compile with only the sync→async call-site change; legacy-only behavior is bit-identical (proved by the compat reader's `legacy-only` test — `readIntake` returns the parsed legacy file unchanged; audreys has no `intake.json`, so a fixture was used)
- [x] Ownership boundary respected (`git diff --name-only` ⊆ owned set; test-infra additions itemized + justified in the Changelog; specs 03/04 untracked WIP not touched)
- [x] Changelog: deviations (readIntake async, conflict guard, timestamp invariant, test-infra), the acceptance transcript, follow-ups (raw readers to repoint, legacy deletion criteria, ClientIntake type retirement)

## Changelog

- 2026-06-04: spec written (tier-2 batch with 03, 04).
- 2026-06-04: **built.** Mirror verified field-for-field (no drift). Shipped: `commands/profile/migrate-intake.ts` (single + `--all`, marker-gated idempotency, before/after print); `util/intake-reader.ts` rewritten as the async profile-first compat reader plus the pure `clientIntakeToAuditDecisions` / `auditDecisionsToClientIntake` transforms and `planMigration`; the three CLI consumers (`fixes/plan`, `clone`, `improve/index`) take the one permitted `await` change; dashboard `intake-writer.writeIntake` dual-writes (profile via `mergeCandidate` + byte-for-byte legacy mirror) and `fs-reader.readIntake` is the profile-first compat read; the two API routes are storage-repointed only (logic byte-identical). CLI suite green (306/306 incl. 13 new intake-reader cases); dashboard 15/15 green; root `pnpm build` clean.

  **Deviations (DoD-flagged):**
  - **`readIntake` went async** (the spec's permitted sync→async). The compat reader must read the profile through the async data source, so the signature is now `Promise<ClientIntake | null>`. Applied at all three CLI call sites as `await readIntake(slug)` — the *only* change at those sites. The dashboard `readIntake` was already async.
  - **Conflict is an explicit guard, not a `mergeCandidate` outcome.** `auditDecisions` is wrapped operator-sourced, and an operator candidate *never* conflicts under `mergeCandidate` (precedence 3 + same-source last-write-wins). To honor the spec's "conflict if the profile already has a differing auditDecisions — do not overwrite" (and the DoD's "conflicting" test), `planMigration` guards directly: absent → apply; deep-equal → no-op; **differing → queue a `ConflictEntry`, leave the profile and `intake.json` untouched, write no marker.** The apply path still routes the build through `mergeCandidate` (empty slot) so the envelope is constructed identically.
  - **Timestamp invariant: `envelope.updatedAt == the ClientIntake's updatedAt`.** Migrate stamps the envelope with the legacy file's `updatedAt` (so the acceptance round-trip is exact); the dashboard `writeIntake` stamps it with the intake's own `updatedAt` (not a fresh `Date.now()`). This is what keeps a reconstruct-from-profile read byte-identical to the legacy mirror and to a POST's own response. `_meta.updatedAt`/`revision` is a separate document-write stamp and does not enter the API contract.
  - **Test infrastructure beyond the literal owned set** (unavoidable — the DoD mandates dashboard tests where none existed). Added: `packages/dashboard/tsconfig.test.json` (NodeNext, `dist-test/`), a `test` script + `dist-test` clean in `packages/dashboard/package.json`, `test/` (`_setup.ts`, `fs-reader.test.ts`, `intake-writer.test.ts`, `route-intake.test.ts`), `test`/`dist-test` excludes in `packages/dashboard/tsconfig.json`, and `dist-test/` in root `.gitignore`. The two API route files' `@/lib/...` imports were repointed to relative `.js` so the route graph compiles+runs under plain `node --test` with no bundler/loader (production astro build still resolves them — verified). CLI tests live in the owned `util/intake-reader.test.ts` (no test file under `commands/`, which oclif scans).

  **Acceptance transcript (temp `UPRIVER_CLIENTS_DIR` seeded with the real littlefriends `profile.json`, so the tracked tree stays clean):**
  - *Fresh:* hand-seeded `intake.json` (3 findingDecisions, 2 pageWants, 1 referenceSite, scopeTier `rebuild`) → `migrate-intake littlefriends` → `profile.auditDecisions` filled, `_meta.revision` 1→2, envelope `source: operator`, envelope `updatedAt` = legacy `2026-05-12T09:30:00Z` (invariant). `readProfile` validates the written profile; `readIntake('littlefriends')` returns the reconstructed `ClientIntake` with `updatedAt` preserved exactly. Original `intake.json` left in place; `intake.json.migrated` marker written (`{migratedAt, slug, profileRevision: 2, source, note}`).
  - *Idempotent re-run:* marker present → "already migrated; skipping", exit 0, revision unchanged.
  - *Conflict:* a profile pre-seeded with a *differing* `auditDecisions` (scopeTier `polish`) + a legacy `intake.json` (scopeTier `rebuild`), no marker → conflict reported, profile untouched (revision 5, scopeTier `polish`), no marker written, one entry queued to `profile-conflicts.json` (`path: auditDecisions`, existing `polish` vs candidate `rebuild`), exit 1.
  - *`--all`:* two clients (`alpha` fresh; `bravo` pre-marked `.migrated`) → `alpha` migrated (profile created, revision 1, marker written), `bravo` skipped, summary `1 migrated, 0 already-present, 1 previously-migrated, 0 conflict(s), 0 invalid`, exit 0.

  **Follow-ups:**
  - **Out-of-scope raw `intake.json` readers to repoint before legacy deletion** (best-effort; they read the *frozen* file so they remain correct during the dual-write freeze window): `deep-audit/passes/content-strategy/run.ts` and `conversion-psychology/run.ts` (read `pageWants` directly), and `commands/report/bundle.ts` (copies raw `intake.json` into the bundle). Repoint these to the compat `readIntake` (or bundle the profile's `auditDecisions`) before the file is deleted.
  - **Legacy `intake.json` deletion** is deferred (per the PRD: after one full client cycle). The `.migrated` marker + a clean `profile-conflicts.json` gate it; the above readers must be repointed first.
  - **`ClientIntake` type retirement** (`@upriver/core`) stays pending until every consumer reads the profile; removing it now would ripple (out of scope this tier).
  - **`--all` enumerates via `listClientSlugs()`**, which is gated on `client-config.yaml`; a slug with an `intake.json` but no config (e.g. littlefriends) is not picked up by `--all` (run it by slug). Noted, not a blocker.
  - **`this.exit(1)` on conflict** matches the repo convention (`clone-links`, `clone-embeds`, `generate`); the `ExitError` stack trace printed by `bin/run.js` (which doesn't wrap `run()` with oclif's `handle`) is a pre-existing, out-of-scope quirk affecting all commands equally.
  - **Env note:** the CLI's existing `pnpm test` glob (`node --test 'dist/**/*.test.js'`) is not expanded by `sh` in this environment; suites were run via `find`-expansion. Pre-existing; CLI `package.json` is not owned by this spec.
  - **Parallel-build collision (spec 03):** during this build, spec 03 flipped the CLI's read-only `generate/data-source.ts` default from `local` to `supabase` (its stated `pull`/`push` change). To stay robust to the env-controlled default, this component's tests now **pin `UPRIVER_DATA_SOURCE=local`** (in `intake-reader.test.ts` and the dashboard `test/_setup.ts`) instead of relying on the default; the acceptance/`--all` runs export `UPRIVER_DATA_SOURCE=local`. Operators running `migrate-intake` locally must set `UPRIVER_DATA_SOURCE=local` (or configure Supabase). No code in this component depends on the default — everything routes through `resolveClientDataSource()`. The dashboard resolver (`lib/data-source.ts`) was not flipped.
