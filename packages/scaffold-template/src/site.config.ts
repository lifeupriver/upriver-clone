// Site-wide constants. `upriver scaffold` overwrites this file with the
// client's business name from the audit package, so the 'Client' stub
// below only survives in unscaffolded template builds.
export const SITE_NAME = 'Client';
